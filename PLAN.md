# PICKFIT (픽핏) — 옷장 코디 추천 웹앱 구현 계획

## Context

내가 가진 옷을 사진으로 찍어 저장해두면, 오늘 날씨와 상황에 맞는 코디를 골라주는 앱을 만든다.
웹페이지 제작 강의 실습 결과물 겸 개인 프로젝트.

현재 저장소는 `README.md` 한 개뿐인 빈 상태 → 전부 새로 만든다.

**앱 이름: PICKFIT** — 워드마크는 `PICKFIT`, 한글 표기는 `픽핏`. UI 문구는 한국어, 로고·타이틀은 영문 워드마크로 간다.
(이름은 `index.html` 타이틀·`manifest.json`·`assets/logo.svg` 세 군데에만 등장하게 짜서 나중에 변경이 쉽도록 한다)

확정된 제약 조건:
- **API 키 없음** — 외부 AI API 없이 브라우저 안에서 최대한 자동화한다
- **내 폰에만 저장** — 서버·로그인 없이 IndexedDB에 사진까지 저장한다
- **추천 기준 4가지 전부** — 오늘 기온 / 상황(TPO) / 색상 조합 / 최근 착용 기록
- **모든 옷은 누끼 처리가 되어야 한다** — 추천 결과를 옷들을 합성한 **코디 조합 이미지**로 보여주기 때문. 누끼 실패 시 등록을 거부하고 재촬영을 요청한다
- **MVP는 클로드코드가 전량 제작** — 기능부터 시각 마감까지 혼자 완결한다. 이미지·일러스트 고급화는 이후 코덱스로 보강

결과물은 앱스토어 심사 없이 링크로 쓰는 **모바일 우선 정적 웹앱(PWA)**. 홈 화면에 추가하면 앱처럼 열리고, 오프라인에서도 옷장과 코디 추천이 동작한다.

### API 없이 자동화되는 범위

| 항목 | 자동 여부 | 방식 |
|---|---|---|
| **배경 제거(누끼)** | **자동 · 필수 통과 요건** | flood-fill → 온디바이스 모델 → 수동 보정. 실패 시 등록 거부 |
| 대표 색상 추출 | 자동 | 누끼된 픽셀만 색상 군집화(k-means) |
| 톤/채도/밝기 | 자동 | HSL 통계 |
| 계절감 추정 | 자동(수정 가능) | 색상 톤·채도 기반 가중치 |
| 보온 점수 | 반자동 | 카테고리 + 두께 칩 1탭 |
| **품목 카테고리** | **탭 2번** | AI 없이는 정확 분류 불가 → 추측값을 기본 선택해 제시 |

→ 옷 1개 등록에 필요한 입력은 **사진 1장 + 탭 2~3번**.

---

## 등록 흐름 — 최우선 설계 과제

이 앱이 실패하는 가장 큰 원인은 기능 부족이 아니라 **옷 20개 등록하다 지쳐서 그만두는 것**이다.
따라서 등록 흐름의 마찰을 없애는 것을 기능보다 우선한다.

**발상 전환**: 앱 안에서 한 장씩 찍는 흐름을 버린다.
웹앱은 카메라를 열어두고 연사하는 게 네이티브만큼 매끄럽지 않다. 대신 **폰 기본 카메라로 몰아 찍고 → 앱에서 앨범 다중 선택으로 일괄 투입**한다. 가장 빠른 촬영 도구(기본 카메라)를 그대로 쓰고 앱은 정리만 담당한다.

**1. 배치 등록** — `<input type="file" accept="image/*" multiple>` 로 20장을 한 번에 받아 자동 분석 큐에 넣는다. "추가 → 촬영 → 저장 → 다시 추가" 왕복을 제거한다.

**2. 묶음 카테고리 일괄 지정** — 사진 투입 **전에** "이번 묶음은 전부 상의"를 1회 선택한다. 옷장은 종류별로 걸려 있어 몰아 찍기가 자연스럽고, 카테고리 입력이 20회 → **0회**가 된다.

**3. 스와이프 확정** — 분석 결과 카드를 스택으로 쌓고 오른쪽=확정 / 왼쪽=버림 / 위=수정. 틀린 것만 골라 고친다.

**4. 누끼는 필수 요건이다 — 통과 아니면 재촬영** — 코디 추천을 **조합 이미지**로 보여주려면 모든 아이템이 투명 배경 PNG여야 한다. 배경이 남은 사진은 레이어로 겹칠 수 없다. 따라서 누끼는 심미 요소가 아니라 **핵심 기능의 전제 조건**이며, 품질 등급은 `quality: 'ok' | 'failed'` **이분법**이다. 중간 등급을 두지 않는다.

이 요건 때문에 **촬영 환경 요구가 올라간다.** "옷장에 걸린 채 찍기"는 허용하지 않는다 — 배경에 다른 옷이 있으면 경계를 찾을 수 없다. **단색 배경 위에 놓고 찍기가 필수**다. 다만 이불·수건 한 장 까는 1회 세팅이므로 몰아 찍기 흐름과는 오히려 잘 맞는다.

**누끼 성공률을 올리는 3단 전략** — 앞 단계가 실패하면 다음 단계로 넘어간다.

| 단계 | 방법 | 성격 |
|---|---|---|
| 1차 | 테두리 flood-fill (Canvas) | 가볍고 즉시. 단색 배경이면 대부분 성공 |
| 2차 | **온디바이스 세그멘테이션 모델** (Transformers.js + ONNX Runtime Web) | 복잡한 배경도 처리. API 키·서버 불필요, 모델 파일만 최초 1회 다운로드 후 캐시 |
| 3차 | 수동 보정 — 사용자가 영역을 대충 지정 | 자동으로 안 될 때의 최후 수단 |
| 실패 | 사유 안내 + 재촬영 요청 | 저장하지 않는다 |

**구현 순서**: 1차 flood-fill을 먼저 만들고 실제 사진 20장으로 성공률을 잰다. 성공률이 낮으면 그때 2차 모델을 붙인다. 처음부터 모델을 넣으면 로딩·캐싱·WebGPU 폴백 문제로 기능 검증이 늦어진다. **단, 2차 도입은 "선택"이 아니라 성공률에 따른 필수 대비책으로 취급한다.**

**누끼 결과물 규격** (조합 이미지 합성을 위해 통일)
- 투명 배경 PNG, **바운딩 박스로 여백을 트림**한 뒤 저장 → 조합 시 배치가 어긋나지 않는다
- 원본 종횡비를 `aspect` 필드로 함께 저장
- 가장자리 1~2px 페더링으로 계단 현상 완화

**5. 누끼 실패 시 명시적 안내와 재촬영** — "실패했습니다"만 띄우면 무엇을 고쳐야 할지 알 수 없다. 원인을 판별해 원인별 해결책을 함께 제시한다.

| 판정 조건 | 사용자에게 보여줄 안내 |
|---|---|
| 배경으로 분류된 비율 < 5% | "배경과 옷 색이 비슷해서 옷을 구분하지 못했어요. **다른 색 배경 위에 놓고** 다시 찍어주세요" |
| 배경으로 분류된 비율 > 90% | "옷을 찾지 못했어요. **옷이 화면 가운데에 들어오게** 다시 찍어주세요" |
| 옷 바운딩 박스가 화면의 3% 미만 | "옷이 너무 작게 담겼어요. **조금 더 가까이서** 찍어주세요" |
| 옷 영역이 화면을 거의 꽉 채움(여백 부족) | "옷 경계를 못 찾았어요. **옷 주변에 여백을 두고** 찍어주세요" |
| 큰 덩어리가 2개 이상으로 분리됨 | "한 장에 옷이 여러 벌 있는 것 같아요. **한 장에 한 벌만** 담아주세요" |
| 평균 밝기가 임계값 이하 | "사진이 너무 어두워요. **밝은 곳에서** 다시 찍어주세요" |

**실패 카드 UI** — 스와이프 스택에서 실패 카드는 경고 톤으로 구분해 표시하고, 위 안내 문구와 버튼 2개를 둔다.
- **[다시 찍기]** (주 버튼) — 그 자리에서 카메라/앨범을 열어 **그 한 장만 교체**한다. 나머지 진행 상황은 유지
- **[직접 잘라내기]** (보조) — 3차 수동 보정으로 진입. 사진은 괜찮은데 자동 판별만 실패한 경우를 구제한다
- **배경이 남은 채로 저장하는 선택지는 두지 않는다.** 조합 이미지가 깨지기 때문이다

**배치 실패분 관리** — 20장을 넣었는데 2장이 실패했다고 전체 흐름이 끊기면 안 된다.
- 성공분은 먼저 저장하고, 마지막에 요약을 보여준다 → **"18벌 등록 완료 · 2장은 다시 찍어주세요"**
- 실패한 사진은 버리지 않고 **재촬영 대기 목록**(별도 IndexedDB 스토어)에 원본과 실패 사유를 함께 보관한다. 어떤 옷이었는지 나중에 봐도 알 수 있고, 짬날 때 그 목록에서 이어서 처리한다
- 옷장 화면 상단에 "다시 찍을 사진 2장" 배너를 띄워 잊지 않게 한다

**6. 미완성 저장 허용** — 태그가 비어도 저장한다. 기본값: 계절=전 계절, TPO=데일리, 보온 점수=카테고리에서 자동 유도(반팔 1 / 셔츠 2 / 니트 3 / 자켓 4 / 코트 5). **최소 입력은 카테고리 1개, 배치 지정 시 0개.** 옷장에 "정보 부족" 배지를 달아 나중에 채우게 한다.

**반드시 처리할 기술 이슈**
- **EXIF 회전** — 처리하지 않으면 폰 사진이 눕는다. `createImageBitmap(blob, { imageOrientation: 'from-image' })` 로 흡수
- **HEIC(아이폰 기본 포맷)** — 안드로이드 브라우저에서 디코딩 실패. 실패 시 "아이폰 설정 → 카메라 → 포맷 → 높은 호환성" 안내를 띄우고 해당 장만 건너뛴다
- **대량 처리 성능** — 20장 연속 분석 시 UI가 멈추지 않게 순차 처리 + 진행률 표시. 원본은 장변 1600px로 리사이즈해 저장(용량 폭증 방지)

**온보딩에 넣을 촬영 요령** (첫 실행 시 3화면)
1. **단색 배경은 선택이 아니라 필수다.** 침대·바닥에 옷과 다른 색의 단색 이불·수건을 깔아 배경막으로 쓴다 (1분 세팅, 계속 재사용). 어두운 옷엔 밝은 배경, 밝은 옷엔 어두운 배경
2. 옷을 하나씩 놓고 위에서 한 장 → 치우고 다음. **옷 주변에 여백을 남기고** 찍는다. 옷 바꾸는 속도가 곧 촬영 속도
3. 종류별로 몰아 찍는다 (배치 카테고리 지정과 맞물림)
4. **최근에 산 옷은 찍지 말고 쇼핑몰 상세페이지 이미지를 저장한다** — 흰 배경 고품질이 공짜로 생긴다
5. 옷장을 다 털지 말고 자주 입는 20벌만 먼저. 추천은 20벌로 충분히 돌아간다

---

## 화면 구성 (5개)

1. **옷장** — 카테고리 필터 탭 + 누끼 썸네일 그리드. 첫 진입 시 빈 상태 안내
2. **옷 추가** — 묶음 카테고리 선택 → 앨범 다중 선택/촬영 → 자동 분석 큐 → 스와이프 확정 → 저장
3. **오늘 코디** — 기온 배지 + 상황 칩(직장/데일리/데이트/운동/포멀) → **옷들을 합성한 코디 조합 이미지** 3개 카드 → "이거 입었어요" 기록
4. **아이템 상세** — 착용 이력, 태그 수정, 삭제
5. **설정** — ZIP 백업 내보내기/복원, 저장 용량 표시, **영구 저장 권한 상태**, 원본 보관 토글, 데이터 보관 안내 문구, 전체 초기화

---

## 파일 구조

```
index.html            단일 진입점, 화면 5개 마크업 템플릿
css/base.css          레이아웃 골격 (grid/flex, 위치, 크기)
css/theme.css         디자인 토큰 값 — 색·타이포·그림자·모션
js/app.js             화면 라우팅 + 렌더링
js/db.js              IndexedDB 래퍼 (items / wearLog / settings)
js/analyzer.js        ★ 사진 자동 분석 (누끼·색상·톤·계절)
js/segment.js         누끼 전담 — flood-fill 1차, 온디바이스 모델 2차, 수동 보정 3차
js/palette.js         HSL→색상명 매핑, 배색 조합 점수 규칙
js/coordinator.js     ★ 코디 생성 + 점수 계산 엔진
js/outfit-image.js    ★ 누끼 PNG들을 합성해 코디 조합 이미지 생성
js/weather.js         Open-Meteo 호출 + 수동 기온 폴백
assets/               아이콘·일러스트 (MVP는 인라인 SVG로 직접 제작)
manifest.json, sw.js, icons/
```

### 교체 가능하게 격리하는 두 지점

나중에 갈아끼울 게 확실한 두 부분은 **파일 경계와 이름을 미리 고정**해서, 교체 시 다른 파일을 건드리지 않게 한다.

**1. 누끼와 사진 분석 — `js/segment.js` · `js/analyzer.js`**
- `segment(imageBlob) → { ok, cutoutBlob, ... }` — 누끼 방식(flood-fill / 모델 / 수동)을 이 파일 안에서만 갈아끼운다. 1.5단계에서 모델을 붙일 때 호출하는 쪽은 손대지 않는다
- `analyze(cutout) → tags` — 나중에 AI 태깅으로 올릴 때 이 파일 내부만 교체한다

둘 다 단일 함수 인터페이스라 실패해도 그 파일 하나만 되돌리면 끝난다.

**2. 시각 자산 — `assets/` + `css/theme.css`**
`base.css`는 토큰을 **참조만** 하고 값을 정의하지 않는다. 색·폰트·그림자 값은 전부 `theme.css`에 모은다.

```css
/* base.css — 참조만 */
.item-card { border-radius: var(--radius-md); background: var(--c-surface); padding: var(--sp-3); }

/* theme.css — 값 정의. 나중에 이 파일만 리터치하면 전체 룩이 바뀐다 */
:root {
  --c-bg: #faf9f7;  --c-surface: #fff;  --c-text: #1a1a1a;  --c-accent: #3b6ef5;
  --sp-1: 4px; --sp-2: 8px; --sp-3: 12px; --sp-4: 20px;
  --radius-sm: 8px; --radius-md: 14px; --radius-full: 999px;
  --font-body: ...; --shadow-card: ...;
}
```

자산은 **파일명을 먼저 확정**하고 MVP 단계에서 단순한 버전을 채워둔다. 나중에 코덱스가 같은 경로에 더 좋은 파일을 덮어쓰면 그대로 교체된다(형식은 SVG·PNG 무엇이든 무관, `<img src>`로 참조).

```
assets/icon-outer.svg  icon-top.svg  icon-bottom.svg  icon-dress.svg
assets/icon-shoes.svg  icon-bag.svg  icon-acc.svg
assets/tab-closet.svg  tab-today.svg  tab-settings.svg
assets/empty-closet.svg        빈 옷장 일러스트 ← 교체 효과가 가장 큰 자산
assets/logo.svg
icons/icon-192.png  icon-512.png   PWA 앱 아이콘
```

---

## 데이터 모델

```js
item = {
  id, createdAt,
  category: 'outer'|'top'|'bottom'|'dress'|'shoes'|'bag'|'acc',
  colors: [{ hex, ratio }],          // 자동
  baseColor: 'navy',                 // 자동 (12색 이름표)
  tone: 'light'|'mid'|'dark',        // 자동
  chroma: 'neutral'|'muted'|'vivid', // 자동
  seasons: ['fall','winter'],        // 자동 추정 → 칩으로 수정
  tpo: ['daily','work'],             // 칩 선택
  warmth: 1..5,                      // 보온 점수
  cutoutBlob,                        // ★ 투명 배경 PNG (여백 트림 완료). 조합 이미지의 재료
  aspect,                            // 트림 후 종횡비 — 조합 시 크기 정규화에 사용
  segMethod: 'floodfill'|'model'|'manual',  // 어느 단계에서 성공했는지 (성공률 측정용)
  sourceBlob                         // 축소 보관본 (아래 정책 참조)
}
wear   = { id, itemIds: [...], date, tpo }
retake = { id, imageBlob, reason, message, createdAt }   // 분석 실패 → 재촬영 대기 목록
```

**원본 보관 정책** — 원본과 누끼를 온전히 둘 다 저장하면 용량이 두 배가 된다. 원본은 **장변 1024px · JPEG 품질 0.75로 축소해 `sourceBlob`으로 보관**한다. 나중에 세그멘테이션 모델을 붙였을 때 재처리하기엔 충분하고, 용량은 절반 이하로 떨어진다. 설정에 "원본 보관 끄기" 토글을 두어 용량이 급하면 비울 수 있게 한다.

**용량 기준선** — 옷 1벌 ≈ 누끼 PNG 200~400KB + 축소 원본 100~200KB ≈ **0.3~0.6MB**. 100벌이면 30~60MB. 설정 화면에 `navigator.storage.estimate()` 로 실제 사용량을 표시한다.

---

## 누끼 (`segment.js`)

**누끼가 실패하면 그 사진은 등록되지 않는다.** 조합 이미지의 재료가 되지 못하기 때문이다.

1. EXIF 회전을 흡수해 로드하고 장변 512px로 리사이즈해 Canvas에 그린다
2. **1차 flood-fill** — 네 테두리 픽셀의 최빈색을 배경으로 잡고, 테두리에서 시작해 색 거리 임계값 안의 픽셀을 연결 확산으로 제거한다. 옷 안쪽의 비슷한 색은 테두리와 연결되지 않으므로 살아남는다
3. **판정** — 배경 비율·바운딩 박스 크기·연결 덩어리 개수·평균 밝기를 검사 (판정 조건은 등록 흐름 섹션의 표)
4. 실패 시 **2차 온디바이스 세그멘테이션 모델**로 재시도 (Transformers.js + ONNX Runtime Web, WebGPU 우선·WASM 폴백)
5. 그래도 실패하면 **3차 수동 보정** UI를 제안하고, 사용자가 거부하면 실패 사유와 함께 재촬영 대기 목록으로 보낸다
6. 성공 시 마스크를 적용해 **바운딩 박스로 여백을 트림**한 투명 PNG를 만들고, 가장자리를 1~2px 페더링한다

반환값: `{ ok: boolean, reason?, message?, cutoutBlob?, aspect?, method? }`

## 자동 분석 로직 (`analyzer.js`)

`segment.js`가 성공한 뒤에만 실행된다. 배경이 제거된 픽셀만 다루므로 색상 정확도가 높다.

1. 누끼 마스크 안쪽 픽셀만 k-means(k=3)로 군집화 → 비중순 대표 색상 배열
2. 최대 비중 색상의 HSL을 12색 이름표(`palette.js`)에 매핑 → `baseColor`
3. 밝기/채도 통계 → `tone`, `chroma`
4. 계절 추정 — 어둡고 저채도 → 가을·겨울 / 밝은 파스텔 → 봄 / 밝고 저채도 → 여름 (기본값, 수정 가능)

반환값: `{ colors, baseColor, tone, chroma, seasons }`

## 코디 추천 로직 (`coordinator.js`)

**조합 생성** — (아우터 optional) + 상의 + 하의 + 신발, 또는 원피스 + 아우터 + 신발

**필터**
- 기온 구간 vs 조합 보온 점수 합계 (예: 12°C → 합계 7~10 허용)
- 선택한 상황(TPO) 태그를 모든 아이템이 만족

**조합 최소 조건** — 상의 + 하의(또는 원피스)만 있으면 코디가 성립한다. **신발·아우터·가방은 있으면 더하고 없으면 뺀다.** 신발이 없다고 추천이 0건이 되면 안 된다.

**점수 (합산 후 상위 3개)**
- 배색 — 톤온톤(+), 뉴트럴 앵커(+), 보색 1쌍(+), 원색 3개 이상 동시 사용(−)
- 최근 착용 — 7일 내 착용(−), 30일 이상 방치(+)
- 다양성 — 소량 랜덤 지터로 매번 같은 조합이 나오지 않게 한다

**추천 0건 처리 (반드시 구현)** — 옷장이 빈약한 초기 사용자는 100% 이 상황을 겪는다. 빈 화면만 보여주면 앱을 지운다.

1. **단계적 완화** — 0건이면 필터를 순서대로 풀고 재시도한다: ① TPO 필터 해제 → ② 기온 허용 폭 ±3 확대 → ③ 계절 필터 해제. 완화해서 찾았으면 **"조건을 조금 넓혀서 찾았어요"** 라고 밝힌다. 조용히 완화하면 추천 이유 문장과 실제가 어긋난다
2. **그래도 0건이면 원인을 짚어준다** — "하의가 없어서 코디를 만들 수 없어요. 하의를 2~3벌 등록해주세요" 처럼 **부족한 카테고리와 필요한 수량**을 알려주고 등록 화면으로 가는 버튼을 둔다
3. **옷장 진척도 표시** — 옷장 화면에 "코디를 만들려면 하의가 더 필요해요" 안내를 상시 노출해 0건 상황에 도달하기 전에 막는다

**추천 이유 문장 자동 생성** — 점수에 기여한 항목을 그대로 문장으로 조립
> "네이비 니트 + 아이보리 팬츠 — 톤 차이가 또렷해서 깔끔하고, 오늘 12°C에 맞는 보온감이에요. 이 니트는 23일째 안 입으셨어요."

## 코디 조합 이미지 (`outfit-image.js`)

추천 결과를 **옷들을 실제로 합성한 한 장의 이미지**로 보여준다. 이 기능 때문에 누끼가 필수 요건이 된다.

**합성 방식** — 각 아이템의 `cutoutBlob`을 Canvas에 레이어로 그린다.

- **크기 정규화 — 카테고리마다 기준 축이 다르다**
  - **세로 기준**(높이를 맞추고 `aspect`로 너비 계산): 상의 100 / 하의 115 / 아우터 110 / 원피스 150
  - **가로 기준**(너비를 맞추고 `aspect`로 높이 계산): **신발 55 / 가방 45 / 액세서리 30**
  신발은 가로로 긴 형태라 높이로 맞추면 화면을 뒤덮는다. 축을 나누지 않으면 조합 이미지가 무너진다
- **배치** — 세로 착장 순서. 상의 위 → 하의 아래(상의 밑단과 살짝 겹침) → 신발 맨 아래 중앙. 아우터가 있으면 상의 왼쪽에 겹쳐 배치, 가방·액세서리는 오른쪽 여백에 작게
- **구성이 빠져도 깨지지 않게** — 신발·아우터가 없는 조합은 그 자리를 비우고 나머지를 세로 중앙 정렬로 재배치한다
- **캔버스** — 정사각(1024×1024), 배경은 `theme.css`의 표면색. 전체를 중앙 정렬하고 가장자리 여백 확보
- **성능** — 추천 카드 3개 × 아이템 4~5개를 매번 합성하면 느리다. 합성 결과를 **조합 ID 기준으로 메모리 캐시**하고, `requestIdleCallback`으로 다음 카드를 미리 그려둔다

**부가 효과 — 코디 이미지 저장/공유**
합성 결과가 이미 한 장의 이미지이므로, "이미지로 저장" 버튼을 붙이면 오늘의 코디를 앨범에 저장하거나 공유할 수 있다. 구현 비용이 거의 없는데 체감 가치가 크다.

**폴백** — 아이템에 `cutoutBlob`이 없는 경우는 설계상 발생하지 않지만(누끼 실패 시 등록 자체가 안 되므로), 과거 데이터 호환이나 백업 복원 시를 대비해 해당 자리에 색상 블록 + 카테고리 아이콘을 그린다.

## 날씨 (`weather.js`)

Geolocation → **Open-Meteo**(API 키 불필요)로 현재 기온·강수 조회.
위치 거부/오프라인 시 기온 슬라이더 수동 입력으로 폴백 → 추천이 절대 막히지 않는다.

---

## 리스크와 대비책

### 1. 저장 데이터가 사라질 위험 ← 이 설계의 최대 약점

서버 없이 브라우저에만 저장하므로, 브라우저가 저장 공간을 회수하면 옷장이 통째로 날아간다.
**특히 iOS Safari는 일정 기간 방문이 없으면 사이트 저장 데이터를 삭제한다.** 옷 100벌을 등록해두고 몇 주 안 열었다가 전부 잃으면 앱은 끝난다. 3중으로 방어한다.

1. **영구 저장 권한 요청** — 첫 등록 성공 직후 `navigator.storage.persist()` 를 호출한다. 승인되면 브라우저가 자동 회수 대상에서 제외한다. 결과를 설정 화면에 "영구 저장: 켜짐/꺼짐"으로 표시한다
2. **홈 화면 추가 유도** — 홈 화면에 추가된 PWA는 데이터가 유지될 가능성이 크게 올라간다. 옷 5벌 이상 등록한 시점에 안내한다 (**iOS는 설치 유도 배너 API가 없으므로 "공유 → 홈 화면에 추가" 그림 안내를 직접 띄운다**. 안드로이드는 `beforeinstallprompt` 사용)
3. **백업 리마인더** — 마지막 백업 이후 등록한 옷이 10벌을 넘거나 30일이 지나면 설정 화면과 옷장 상단에 백업 권유 배지를 띄운다

**설정 화면에 이 위험을 솔직히 적는다.** "이 앱은 사진을 이 기기에만 저장합니다. 기기를 바꾸거나 브라우저 데이터를 지우면 사라지니 가끔 백업해주세요."

### 2. 백업 파일이 너무 커지는 문제

사진을 JSON에 base64로 담으면 용량이 33% 더 불어나 100벌 백업이 100MB를 넘는다. 무거워서 안 쓰게 되면 위 1번 방어가 무너진다.

- **무압축 ZIP(store 방식)으로 묶는다** — 외부 라이브러리 없이 순수 JS로 구현 가능하다. 이미 압축된 PNG·JPEG라 압축률 이득이 없으므로 store로 충분하다. 구조: `meta.json` + `images/<id>.png`
- **백업 범위 선택** — "누끼만"(가벼움, 앱 복원에 충분) / "원본 포함"(무거움) 중 선택하게 한다. 기본값은 **누끼만**
- 복원은 같은 ZIP을 읽어 IndexedDB에 되넣는다. 복원 시 기존 데이터와 **병합할지 덮어쓸지** 묻는다

### 3. 그 외 대비

- **분석 큐 중단** — 20장 처리 중 화면을 벗어나면 진행 상태를 유지하고 돌아왔을 때 이어서 처리한다. 처리 완료분은 즉시 저장해 다시 하지 않는다
- **캐시 무효화** — 아이템을 수정·삭제하면 그 아이템이 포함된 코디 조합 이미지 캐시를 함께 버린다
- **개발용 시드 데이터** — 실제 옷 사진 없이는 UI를 만들 수 없다. 개발 초기에 단색 도형으로 만든 더미 아이템 12개(상의4·하의4·신발2·아우터2)를 주입하는 함수를 `db.js`에 두고, 설정 화면 숨은 버튼으로 호출한다. 배포 전 제거하지 않고 남겨둬도 무해하다

---

## 진행 순서 (MVP 전량 제작)

| 단계 | 내용 |
|---|---|
| 1 | `index.html` · `css/*` · `db.js`(+**시드 데이터**) · **`segment.js` 1차 flood-fill** · `analyzer.js` + **배치 등록 흐름**(다중 선택 → 묶음 카테고리 → 분석 큐 → 스와이프 확정) + 옷장 그리드 |
| **1.5** | **누끼 성공률 측정 → 필요 시 2차 온디바이스 모델·3차 수동 보정 추가** (아래) |
| 2 | `coordinator.js`(+**추천 0건 처리**) + `outfit-image.js` + 오늘 코디 화면 + 착용 기록 |
| 3 | 날씨 연동, 코디 이미지 저장/공유, **ZIP 백업·복원**, **영구 저장 권한·홈 화면 추가 유도**, PWA(오프라인) |
| 4 | **시각 마감** — SVG 아이콘 세트, 빈 상태 일러스트, 로딩·전환 모션, 앱 아이콘. 그대로 써도 부끄럽지 않은 수준까지 |
| — | **MVP 완료 → 배포** |
| 이후 | (코덱스) `assets/` 이미지·일러스트 고급화, `theme.css` 리터치 |
| 선택 | AI 카테고리 자동 인식 도입 — 아래 참조 |

### 작업 방식

**빠르게** — 한 단계에 필요한 파일은 **한 번에 몰아서 작성**한다. 파일 하나 만들고 확인, 또 하나 만들고 확인하는 식으로 끊지 않는다. 확인은 단계가 끝나는 지점에서 한 번만 한다.

**단순하게** — 빌드 도구·프레임워크·외부 라이브러리를 쓰지 않는다. 순수 HTML/CSS/ES 모듈로만 만든다. 설치할 것도, 깨질 것도 없어서 가장 빠르고 나중에 손대기도 쉽다. ZIP 백업처럼 라이브러리를 부르고 싶어지는 지점도 직접 구현한다(무압축 ZIP은 짧다).

**체계적으로** — 아래 순서를 지킨다. 앞 단계가 검증되기 전에 뒤 단계를 만들지 않는다.

| 지점 | 하는 일 |
|---|---|
| 단계 시작 | 그 단계의 파일 전부 작성 |
| 단계 끝 | 로컬 서버 띄우고 폰으로 직접 확인 → **커밋** |
| 게이트(1.5) | 누끼 성공률 측정 → 수치로 다음 행동 결정 |
| 막힘 | 혼자 우회하지 않고 즉시 보고 후 판단 요청 |

**과설계 금지** — 계획에 없는 기능은 만들지 않는다. "나중에 쓸지도 모르니까" 하는 추상화도 하지 않는다. 교체가 예정된 두 지점(`segment.js`, `analyzer.js`)만 인터페이스를 격리하고, 나머지는 가장 직접적인 방법으로 쓴다.

### 1.5단계 — 누끼 성공률 게이트 (진행 조건)

**누끼가 이 앱의 기술적 급소다.** 1단계 직후 실제 옷 사진 20장으로 flood-fill 성공률을 측정하고, 결과에 따라 진행을 가른다.

| 성공률 | 조치 |
|---|---|
| 90% 이상 | 그대로 2단계 진행. 수동 보정만 붙인다 |
| 70~90% | 3차 수동 보정을 먼저 붙이고 2단계 진행. 2차 모델은 배포 후 판단 |
| 70% 미만 | **2차 온디바이스 세그멘테이션 모델을 여기서 붙인다.** 이 상태로 밀고 나가면 등록이 계속 실패해 앱을 못 쓴다 |

`segMethod` 필드에 어느 단계에서 성공했는지 기록해두므로 성공률은 데이터로 확인할 수 있다.

### 선택 단계 — AI 카테고리 자동 인식 도입 (MVP 완성 후)

품목 카테고리 자동 인식을 붙이는 두 가지 경로. **1안을 권한다.**
(누끼용 세그멘테이션 모델을 이미 붙였다면 ONNX 런타임이 이미 올라와 있으므로 추가 비용이 적다.)

**1안: 온디바이스 모델 — API 키 불필요 (권장)**
- Transformers.js + CLIP 계열 모델을 브라우저에서 실행해 zero-shot 분류
- 키·서버·비용 전부 불필요하고, **사진이 기기 밖으로 나가지 않아 "내 폰에만 저장" 원칙이 유지된다**
- 비용은 첫 실행 시 모델 다운로드 수십 MB(캐시 후 재사용), 사진 1장 추론 1~3초
- 정확도 — 대분류(상의/하의/아우터/신발/가방)는 양호, 세분류(니트 vs 스웨트셔츠)는 부정확
  → **"AI 추측 → 사용자 1탭 확정"** 흐름을 유지한다. 자동 확정하지 않는다
- 구현 시 주의: 모델 로딩 지연 표시, WebGPU 미지원 기기 WASM 폴백, 로딩 실패 시 기존 수동 입력으로 폴백

**2안: 외부 AI API — 키 필요**
- Claude·GPT 비전으로 태깅. 세분류 정확도와 설명 품질이 가장 높다
- **브라우저에 키를 두면 노출된다** → Vercel 서버리스 함수 등 프록시를 두고 키를 숨겨야 한다
- 옷 사진이 외부 서버로 전송되므로 "내 폰에만 저장" 원칙이 일부 깨진다. 도입 시 사용자에게 명시한다

**코디 추천 문장은 AI로 바꾸지 않는다.** 규칙 엔진이 조립하는 문장은 근거가 항상 실제 데이터와 일치한다. 언어모델은 옷장에 없는 조합을 그럴듯하게 말하는 실수를 하므로, 추천의 신뢰도 면에서 규칙 기반이 낫다.

---

## 검증 방법

ES 모듈을 쓰기 때문에 `file://` 직접 열기로는 동작하지 않는다. 로컬 서버로 확인한다.

```
npx serve .          # 또는  python -m http.server 8000
```

1. **폰 실기기** — 같은 와이파이에서 `http://<PC IP>:3000` 접속, 카메라로 실제 옷 촬영 → 등록
2. **누끼 성공률 측정 (최우선 관문)** — 실제 옷 20장을 단색 배경 위에서 찍어 flood-fill 성공률을 잰다. 결과에 따라 1.5단계 게이트 표대로 진행을 가른다. 누끼 결과는 **투명 배경으로 확인**한다(체크무늬 배경 위에 올려 경계 확인)
3. **실패 안내 확인** — 실패를 일부러 유도해 원인별 문구가 맞게 뜨는지 본다. 검은 옷을 검은 이불 위에 놓기 / 한 장에 두 벌 담기 / 어두운 곳에서 찍기로 각각 재현. **[다시 찍기]로 그 한 장만 교체되고 나머지 진행이 유지되는지**가 합격 기준
4. **배치 등록 실전 테스트** — 실제로 옷 15~20벌을 몰아 찍어 **한 번에 투입**한다. 등록을 끝까지 마칠 만큼 편한지가 이 앱의 성패다. 도중에 지치면 흐름을 다시 설계한다. 함께 확인할 것: 20장 연속 분석 중 UI가 멈추지 않는지, 진행률이 보이는지, 아이폰 HEIC 사진이 처리되는지, 사진이 눕지 않는지(EXIF)
5. **실패분이 흐름을 끊지 않는지** — 실패 사진을 일부러 섞어 20장을 투입한다. 성공분이 먼저 저장되고 "18벌 등록 완료 · 2장은 다시 찍어주세요" 요약이 나오는지, 실패분이 재촬영 대기 목록에 사유와 함께 남는지, 옷장 배너로 다시 찾아갈 수 있는지 확인
6. **코디 조합 이미지** — 최소 상의 3 / 하의 3 / 신발 2 / 아우터 2를 등록해 실제 합성 결과를 본다. **청바지가 니트보다 작게 나오지 않는지(크기 정규화), 겹침이 어색하지 않은지, 누끼 가장자리가 지저분하지 않은지**를 눈으로 확인한다. 여기가 앱의 얼굴이다
7. **코디 추천** — 기온·상황 칩을 바꿔가며 추천이 실제로 달라지는지, 추천 이유 문장이 조합과 일치하는지 확인
8. **추천 0건 상황** — 상의만 3벌 등록한 빈약한 옷장을 일부러 만들어본다. 빈 화면이 아니라 **"하의가 없어서 코디를 만들 수 없어요"** 안내와 등록 버튼이 나오는지, 조건 완화로 찾았을 때 그 사실을 밝히는지 확인
9. **착용 기록** — "이거 입었어요" 기록 후 해당 아이템이 다음 추천에서 밀려나는지 확인
10. **오프라인** — 비행기 모드에서 새로고침 → 옷장·코디가 그대로 동작하고 기온만 수동 입력으로 전환되는지 확인
11. **저장 지속성과 백업** — 브라우저 완전 종료 후 재접속 시 데이터 유지 확인. `navigator.storage.persist()` 승인 여부가 설정 화면에 표시되는지, **ZIP 백업 → 전체 초기화 → 복원 왕복**이 사진까지 온전한지 확인. 옷 20벌 기준 백업 파일 크기도 함께 잰다
12. **교체 가능성 확인** — `theme.css` 토큰 값을 바꿔봐서 레이아웃이 깨지지 않는지, `assets/` 파일을 다른 이미지로 덮어써도 정상 표시되는지 확인 (나중 코덱스 보강이 무사히 되리라는 증거)
13. **배포** — Vercel 또는 GitHub Pages에 정적 배포 후 폰에서 "홈 화면에 추가" → 앱처럼 열리는지 확인. **iOS는 설치 배너가 안 뜨므로 수동 안내가 제대로 보이는지** 별도로 확인
